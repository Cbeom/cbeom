extends home
include mixins/note
block content
    form(method="GET")
        input(type="text",name="title",placeholder="Search Title")
        input(type="submit", value="Search")
    hr
    if note
        each item in note
            +note(item)